RANDOM_STATE=42

DATA_PATH=data/mnist.npz
# Outputs
OUTPUT_DIR=reports/phase1_models_notebook

# Phase 1 classes
CLASS_A=0
CLASS_B=1

# Split ratios
train_size=0.6
TEST_SIZE=0.2
VAL_SIZE=0.2

# Feature modes shared by all models
FEATURES=flatten,pca,hog

# Shared feature config
PCA_COMPONENTS=0.95

# Models to run in notebook pipeline
MODELS=knn,logreg,svm

# KNN search
K_VALUES=1,3,5,7,9
